<view class="photo">
    <view class="add" bindtap="upfile">
        <span>+</span>
        <view>上传图片</view>
    </view>
    <view class="list">
        <block wx:for="{{list}}" wx:key="{{idx}}">
            <image class="img" src="{{item}}" /> 
        </block>
    </view>
</view>